interface:
  display_name: "Codex Collaboration Conductor"
  short_description: "Bounded native collaboration with parent acceptance"
  default_prompt: "Use $codex-collab-conductor when this task has at least two independent native-subagent lanes or needs bounded implementation followed by a deferred fresh second opinion; keep final acceptance with the parent."
policy:
  allow_implicit_invocation: true
